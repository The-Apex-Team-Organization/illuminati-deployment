apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scheduler-stage
  namespace: illuminati
spec:
  project: default

  sources:
    - repoURL: https://github.com/The-Apex-Team-Organization/illuminati-helm-charts
      targetRevision: main
      path: scheduler
      helm:
        valueFiles:
          - $values/vars/stage/scheduler/values.yaml

    - repoURL: https://github.com/The-Apex-Team-Organization/illuminati-deployment.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: illuminati

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
